# M2 候选机制保留与准入

## 最终分类补记（2026-09-09）

完整审计随 M1 的 `docs/COMPONENT_AUDIT.md` 保存（当前工作树
`E:\M1-research-recovery`，尚未推送，不假设远程已有）。
M2优先研究融合式预测编码液态核＋生成式重放，其次为选择性转移与可更新记忆；
目标反馈/有限工作区后置，多神经调制总线暂缓。Transformer只作强对照，
现代化训练组件只作工程基础，不以复制attention主干作为创新目标。

Hamiltonian头、辛积分与物理轨迹基准归Physics；当前只是归属决定，未搬源码。
未接线的top-down/神经调制不是训练判负。融合核、外挂PC、存储固化、
生成式重放、窗口内fast-weight、持久fast-weight各自记账，不互借结论。
不恢复已撤回主张，不自动启用旧开关，不直接训练2B。

日期：2026-09-09。Owner：Everest。当前运行时基准：8409311。

## 重新列入候选：融合式预测编码液体核

源：everest-an/M1，提交 eb6493c；最新核对版本 c3f3008。
代码 `experiments/liquid_pc/model.py` 的 `PCLiquidCore`，证据
`experiments/liquid_pc/report_nchain_continual.json`。

这是独立时序核，不是 M2 的 `mt_lnn/research/predictive_coding.py` 辅助模块。
自上而下预测、误差传递、液体状态更新构成核本身。主LM外挂PC的PPL负结果，
不能直接否定它；反过来也不能用它的时序结果宣称主LM已经获得能力。

旧五任务实验：五种子、40 epochs、序列96、buffer16。平均遗忘PC 0.552、
GRU 0.956；最差任务MSE 0.846 vs 1.383，两个指标5/5配对更低。
范围仅为小型合成时序；双方明显退化，五个种子不证明普遍稳定性或有界失败。

## 后续任务与验收

1. 先登记旧代码、数据、版本和依赖；迁移时保留M1原始副本，附数值等价测试。
   当前只登记候选，尚未把该实验接入M2训练器。
2. 先核查旧结果、任务顺序和数据泄漏，再决定是否需要复现；有完整可信结果不重跑。
3. 新实验同时包含真实回放、生成式回放、无回放对照；相同容量/数据/预算。
   主指标预注册最差任务、平均遗忘、新任务学习质量；报告逐seed和失败率。
4. 扩展到真实序列并测试任务顺序敏感性。仅在新任务和对照上复现收益才考虑语言模型接入。

## 其他候选与关闭项

- selective exp：保留 M1 parity_exp_param.json 的机制证据，不把普通文本收益当已成立。
- 循环推理：旧判决未支持多想更准；新方案先通过因果性与分块/逐token一致性检查。
- snap/STE、全开生物模块、恢复旧宣传数字：不进入训练队列。
- 不将生成式重放称为PC独有；GRU也可以生成重放数据。

## 工程门槛

旧导出仍有指向已搬走模块的项目，需要独立修复。文本入口目前只开放
baseline/selective_state；未放行循环文本训练。2B仅有配置，不能写成训练完成。
M1与M2使用各自环境、训练入口和结果目录，不跨仓写checkpoint。

本轮仅登记计划和证据边界，没有迁移模型实现或启动训练。
